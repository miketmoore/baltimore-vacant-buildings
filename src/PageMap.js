var React = require('react');
var Layout = require('./Layout');

module.exports = React.createClass({
    render () {
        return (
            <Layout>
                <h4>Map TODO</h4>
            </Layout>
        )
    }
})
